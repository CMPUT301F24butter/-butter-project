<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Normal state -->
    <item android:state_pressed="false" android:drawable="@android:color/white" />
    <!-- Selected state -->
    <item android:state_selected="true" android:drawable="@color/secondaryGreyColor" />
    <!-- Pressed state -->
    <item android:state_pressed="true" android:drawable="@color/secondaryGreyColor" />
</selector>